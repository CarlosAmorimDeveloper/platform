import { ALL_PRIORITIES, PRIORITY_LABELS, PRIORITY_COLORS } from './ticketPriority';
import type { TicketPriority } from './ticketPriority';

describe('ticketPriority', () => {
  it.todo('ALL_PRIORITIES contains all 5 priority levels');
  it.todo('PRIORITY_LABELS has label for every priority');
  it.todo('PRIORITY_COLORS has color for every priority');
});
