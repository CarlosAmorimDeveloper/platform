import { login } from './authService';

describe('authService', () => {
  it.todo('login calls firebase signInWithEmailAndPassword');
  it.todo('login maps firebase errors correctly');
  it.todo('register creates user in firestore');
  it.todo('sendPasswordReset calls firebase sendPasswordResetEmail');
});
