import React from 'react';
import { render } from '@testing-library/react-native';
import { Register } from './Register';

describe('Register', () => {
  it.todo('renders name, email and password fields');
  it.todo('shows password validation error for short password');
  it.todo('shows admin notice for first user');
  it.todo('navigates back to Login on button press');
  it.todo('disables submit button when fields are invalid');
});
