import { useTicketList } from './useTicketList';

describe('useTicketList', () => {
  it.todo('returns empty list initially');
  it.todo('filters tickets by status');
  it.todo('updates on new ticket subscription');
});
