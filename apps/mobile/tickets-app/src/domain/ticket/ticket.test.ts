import { toTicket, formatDate } from './ticket';

describe('ticket domain', () => {
  it.todo('toTicket maps Firestore document to Ticket');
  it.todo('toTicket handles missing fields with defaults');
  it.todo('formatDate formats Timestamp to pt-BR locale');
  it.todo('formatDate returns empty string for null');
});
