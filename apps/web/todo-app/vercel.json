{
  "buildCommand": "cd ../../.. && npx turbo run build --filter=todo-app",
  "installCommand": "cd ../../.. && yarn install",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."
}
